// TODO: Game session class. Holds game board data, player data, etc.
export class GameSession {
    constructor(colTags: string[], rowTags: string[]    ) {
        console.log("Starting local session!");
        console.log(colTags, rowTags);
    }
}

